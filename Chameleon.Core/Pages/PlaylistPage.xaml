<?xml version="1.0" encoding="utf-8" ?>
<core:MvxContentPage
    xmlns:core="clr-namespace:MvvmCross.Forms.Views;assembly=MvvmCross.Forms" 
    xmlns:mvx="clr-namespace:MvvmCross.Forms.Bindings;assembly=MvvmCross.Forms"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:Chameleon.Core.ViewModels;assembly=Chameleon.Core"
    xmlns:mm="clr-namespace:MediaManager.Forms;assembly=MediaManager.Forms"
    xmlns:pan="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
    xmlns:templates="clr-namespace:Chameleon.Core.Templates;assembly=Chameleon.Core"
    mc:Ignorable="d"
    x:TypeArguments="viewModels:PlaylistViewModel"
    x:Class="Chameleon.Core.Views.PlaylistPage">


    <ContentPage.Content>
        <StackLayout>
            <SearchBar
                mvx:La.ng="Placeholder SearchBar"
                Text="{mvx:MvxBind SearchText}"/>
            <Label mvx:La.ng="Text Title" Style="{StaticResource Header1}" Margin="24,0,24,14"/>
            <Grid Padding="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="114"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="114"/>
                </Grid.RowDefinitions>
                <pan:PancakeView
                    CornerRadius="18"
                    IsClippedToBounds="True"
                    HeightRequest="114"
                    WidthRequest="114">
                    <pan:PancakeView.HasShadow>
                      <OnPlatform x:TypeArguments="x:Boolean"
                                  iOS="False"
                                  Android="True" />
                    </pan:PancakeView.HasShadow>
                    <AbsoluteLayout>
                        <Image
                        HorizontalOptions="Center"
                        VerticalOptions="Center"
                        Aspect="AspectFill"
                        Source="cover_art_placeholder"/>
                        <Image Source="{mvx:MvxBind PlayPauseImage}"
                               AbsoluteLayout.LayoutFlags="All"
                               AbsoluteLayout.LayoutBounds=".5,.5,.5,.5"/>
                    </AbsoluteLayout>
                </pan:PancakeView>
                <StackLayout Margin="18,4,0,4" Grid.Column="1">
                    <Label
                        Text="Deze titel is extra lang gemaakt zodat je niet meer kunt zien wat er staat"
                        MaxLines="2"
                        LineBreakMode="TailTruncation"
                        FontFamily="FiraSans#600"
                        FontAttributes="Bold"
                        TextColor="#FFFFFFFF"
                        FontSize="24"/>
                    <Label
                        Text="45 tracks"
                        VerticalOptions="EndAndExpand"
                        FontFamily="FiraSans#700"
                        FontSize="12"
                        TextColor="#FFFFFFFF"/>
                    <Label
                        Text="2 hours, 23 minutes"
                        FontFamily="FiraSans#700"
                        FontSize="12"
                        TextColor="#FFFFFFFF"/>
                </StackLayout>
            </Grid>
            
            <CollectionView x:Name="MyCollectionView"
                            ItemsSource="{mvx:MvxBind MediaItems}"
                            SelectionMode="Single"
                            SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                            SelectionChangedCommand="{Binding PlayCommand}"
                            SelectionChangedCommandParameter="{Binding Source={x:Reference MyCollectionView}, Path=SelectedItem}"
                            VerticalOptions="FillAndExpand">
                <CollectionView.EmptyView>
                    <StackLayout>
                        <ActivityIndicator Color="DimGray" IsRunning="True" HeightRequest="50" />
                    </StackLayout>
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <templates:MediaItemTemplate/>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</core:MvxContentPage>
